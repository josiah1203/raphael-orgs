openapi: 3.1.0
info:
  title: raphael-orgs
  version: 0.1.0
  description: Organizations, memberships, invites, billing hierarchy
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
